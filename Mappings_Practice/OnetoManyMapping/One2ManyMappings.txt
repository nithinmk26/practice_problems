class User {
	
	@Id
	@GeneratedValue(strategy = GenerationType.AUTO)
	@Column(name = "user_id")
	private int userId;
	
	private String userName;
	
	private String city;
	
	@OneToMany(cascade = CascadeType.ALL)
	@JoinColumn(name = "user_id", referencedColumnName = "user_id")  //Creates the user_id name as FK in UserContact Table 
	private List<UserContact> contactList;
	
	
	
	
	
	
class UserContact {
	
	@Id
	@GeneratedValue(strategy = GenerationType.AUTO)
	@Column(name = "contact_id")
	private int contactId;
	
	private Long contactNumber;
	
	
	
	
use mappings_practice;

show tables;

drop database mappings_practice;

desc user_data;

desc user_contact;



select  * from user_contact;

select  * from user_data;