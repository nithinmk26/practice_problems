@Entity
@Table(name = "user_data",catalog = "mappings_practice")
public class User {
	
	@Id
	@GeneratedValue(strategy = GenerationType.AUTO)
	@Column(name = "user_id")
	private int userId;
	
	private String userName;
	
	private String city;
	
	@OneToMany(cascade = CascadeType.ALL)
	@JoinColumn(name = "user_id", referencedColumnName = "user_id")
	private List<UserContact> contactList;
	
	
	
	
	
	contact_id	int(11)	NO	PRI		auto_increment
contact_number	bigint(20)	YES			
user_id	int(11)	YES	MUL		
	
	
	
	
 */
@Entity
@Table(name = "user_contact")
public class UserContact {
	
	@Id
	@GeneratedValue(strategy = GenerationType.AUTO)
	@Column(name = "contact_id")
	private int contactId;
	
	private Long contactNumber;
	
	
	
	
	user_id	int(11)	NO	PRI		auto_increment
city	varchar(255)	YES			
user_name	varchar(255)	YES			
	
	
	
@PostMapping("/register")
	public ResponseEntity<User> registerPerson(@RequestBody User person){
//		for (UserContact iterable_element : person.getContactList()) {
//			iterable_element.setUser(person);
//		}
		User userSaved = userRepository.save(person);
		
		return new ResponseEntity<>(userSaved,HttpStatus.CREATED);
	}
	
	
	
	
	
	
	
	
	
	
{
	"userName":"Checthu",
	"city":"Bangalore",
	"contactList":[
		{
			"contactNumber":9113683980
		},
		{
			"contactNumber":9113683989
		},
		{
			"contactNumber":9113683988
		}
		]
}